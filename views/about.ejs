<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Blog</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <header>
        <nav>
            <ul class="navbar">
                <li><a href="/">Home</a></li>
                <li><a href="/about">About</a></li>
                <li><a href="/projects">Projects</a></li>
                <li><a href="/contact">Contact</a></li>
            </ul>
        </nav>
    </header>
    <h1>Welcome to Our Blog!</h1> 
    <p>This blog shares insights, stories, and experiences.</p>
    <p>Stay tuned for our latest posts!</p> 
    <ul class="post_lists">
        <% if (blogs.length > 0) { %>
            <% blogs.forEach(blog => { %>
                <h2><%= blog.title %></h2>
                <p><%= blog.content %></p>
            <% }) %>
        <% } else { %>
            <p>No blog posts found.</p>
        <% } %>
    </ul>
</body>
</html>